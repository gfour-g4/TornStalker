DISCORD_TOKEN=your-discord-bot-token
OWNER_DISCORD_ID=your-discord-user-id
TORN_API_KEY=your-torn-api-key

# Optional initial seed list (only used if no store.json exists yet)
USER_IDS=2796054,123456,789012

# Default fields used when seeding from USER_IDS (comma-separated)
WATCH_FIELDS=description,state

# Polling interval (ms); can be changed at runtime with /watch interval
REQUEST_INTERVAL_MS=5000

# Self-ping URL for Render keepalive
SELF_PING_URL=https://your-render-service.onrender.com/

# If you set this, slash commands will be registered in this guild immediately.
# If not set, commands are registered globally (takes up to ~1 hour to appear).
GUILD_ID=your-guild-id

# Where to store local data (JSON). On Render’s free tier this resets on redeploy.
PERSIST_PATH=./store.json