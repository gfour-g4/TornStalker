DISCORD_TOKEN=your-discord-bot-token
OWNER_DISCORD_ID=your-discord-user-id
TORN_API_KEY=your-torn-api-key

# Comma-separated Torn user IDs to watch
USER_IDS=2796054,123456,789012

# Which status fields to watch (comma separated). Default: description,state
WATCH_FIELDS=state,details,color,until,travel_type,plane_image_type

# How often to check the next user in the list (ms). Smaller = faster checks.
# Example: 5000ms => one request every 5s. With 20 users, each is checked ~every 100s.
REQUEST_INTERVAL_MS=5000

# Self-ping URL (set this to your Render service URL after deploy)
SELF_PING_URL=https://your-render-service.onrender.com/

# Render injects PORT automatically; no need to set it locally